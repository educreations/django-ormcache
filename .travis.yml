language: "python"
python:
   - "2.7"
   - "pypy"
install:
  - pip install -r requirements.txt --use-mirrors
  - pip install coveralls
script:
   "coverage run runtests.py"
after_success:
   "coveralls"
